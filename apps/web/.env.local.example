# Phase 2: Gateway-based routing (all traffic through NGINX reverse proxy)
# Leave empty for same-origin routing through gateway
NEXT_PUBLIC_API_BASE_URL=
# WebSocket URL for real-time updates (routed through gateway)
NEXT_PUBLIC_WS_BASE_URL=